name:  Deploiement alpine avec nginx
on:
  push:
    branches: [ "main" ]
    paths:
      -
jobs:
 site/**
  deploy-site:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      -name: Déploiement du site sur les serveurs Web
        run: |
          for ip in ${{ secrets.WEB1_IP }}; do
            scp -r site/* ubuntu@$ip:/var/www/html/
            ssh ubuntu@$ip 'sudo systemctl reload nginx'
         run: |
          chmod +x scripts/deploy_vm.sh
          ./scripts/deploy_vm.sh
          done

